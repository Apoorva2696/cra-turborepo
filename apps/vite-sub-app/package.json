{
  "name": "vite-sub-app",
  "private": true,
  "version": "0.0.0",
  "main": "./src/App.jsx",
  "scripts": {
    "dev": "tsup ./src/main.jsx --format esm,cjs --watch --external react",
    "build": "tsup ./src/main.jsx --format esm,cjs --external react",
    "preview": "vite preview",
    "lint": "TIMING=1 eslint src/**/*.jsx --fix",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "ui": "*"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^1.0.7",
    "config": "*",
    "vite": "^2.8.0"
  }
}